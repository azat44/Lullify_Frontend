name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

jobs:
  android:
    name: Build signed Android (APK + AAB)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.44.4"

      - run: flutter pub get

      - name: Decode keystore
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > android/app/upload-keystore.jks

      - name: Create key.properties
        run: |
          cat > android/key.properties <<EOF
          storeFile=upload-keystore.jks
          storePassword=${{ secrets.STORE_PASSWORD }}
          keyPassword=${{ secrets.KEY_PASSWORD }}
          keyAlias=${{ secrets.KEY_ALIAS }}
          EOF

      - name: Build release APK
        run: flutter build apk --release --dart-define=API_BASE_URL=https://lullify.up.railway.app/api/v1

      - name: Build release App Bundle
        run: flutter build appbundle --release --dart-define=API_BASE_URL=https://lullify.up.railway.app/api/v1

      - name: Upload Android artifacts
        uses: actions/upload-artifact@v4
        with:
          name: lullify-android
          path: |
            build/app/outputs/flutter-apk/app-release.apk
            build/app/outputs/bundle/release/app-release.aab

  ios:
    name: Build iOS
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.44.4"

      - run: flutter pub get

      - name: Build unsigned archive
        run: flutter build ipa --release --no-codesign --dart-define=API_BASE_URL=https://lullify.up.railway.app/api/v1

      - name: Zip iOS archive
        run: |
          cd build/ios/archive
          zip -r -q lullify-ios.xcarchive.zip *.xcarchive

      - name: Upload iOS archive
        uses: actions/upload-artifact@v4
        with:
          name: lullify-ios-unsigned
          path: build/ios/archive/lullify-ios.xcarchive.zip

  release:
    name: Publish GitHub Release
    needs: [android, ios]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: List downloaded files (debug)
        run: find artifacts -type f

      - name: Create/Update Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/app-release.apk
            artifacts/app-release.aab
            artifacts/lullify-ios.xcarchive.zip